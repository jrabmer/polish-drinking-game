{
  "_comment": "List of board configurations the game offers in the setup dropdown. Add an entry per file you drop into this folder. 'file' is resolved relative to config/.",
  "configs": [
    { "file": "classic.json", "name": "Classic spiral (70 fields)" }
  ]
}
